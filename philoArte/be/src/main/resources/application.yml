server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://112.169.196.99:3306/arte?allowPublicKeyRetrieval=true
    username: arte
    password: Arte1234
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  servlet:
    multipart:
      enabled: true
      location: /Users/superstar_pkb/project/BitCamp Team Project V2/ArtFile
      max-request-size: 30MB
      max-file-size: 30MB

security:
  jwt:
    secret: secret-value

shop:
  upload:
    path: /Users/superstar_pkb/project/BitCamp Team Project V2/ArtFile

UserController:
  signup: Creates user and returns its JWT token
  signin: Authenticates user and returns its JWT token